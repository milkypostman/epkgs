[core]
	repositoryformatversion = 0
	filemode = true
	bare = false
	logallrefupdates = true
[elm]
	mainfile = lisp/dvc-core.el
	# NOTE: XEmacs only
	exclude-required = un-define
	# NOTE: Some of the bundled packages are not available (at least
	# not mirrored) seperately do we can't exclude the complete
	# directory yet.
	# exclude-path = ^lisp/contrib
	exclude-provided = elunit
	# exclude-provided = ewoc
[remote "mirror"]
	url = git@github-elm:emacsmirror/dvc.git
	fetch = +refs/heads/*:refs/remotes/mirror/*
[branch "master"]
	elm-fetcher = bzr
	elm-homepage = http://download.gna.org/dvc/
