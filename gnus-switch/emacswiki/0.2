(:summary    "Smart switch with gnus buffers"
 :created    "20081208"
 :updated    "20081220"
 :license    "GPL-3"
 :authors    ((nil . "andy stewart lazycat.manatee@gmail.com"))
 :maintainer (nil . "andy stewart lazycat.manatee@gmail.com")
 :provided   (gnus-switch)
 :keywords   ("gnus" "switch" "smart")
 :homepage   "http://www.emacswiki.org/GnusSwitch"
 :wikipage   "http://www.emacswiki.org/GnusSwitch"
 :commentary "\
Smart switch with gnus buffers


Installation:

Put gnus-switch.el to your load-path.
The load-path is usually ~/elisp/.
It's set in your ~/.emacs like this:
(add-to-list 'load-path (expand-file-name \"~/elisp\"))

And the following to your ~/.emacs startup file.

(require 'gnus-switch)

No need more.")
