(:summary    "Strip gnus summary buffer"
 :created    "20081208"
 :updated    "20081208"
 :license    "GPL-3"
 :authors    ((nil . "andy stewart lazycat.manatee@gmail.com"))
 :maintainer (nil . "andy stewart lazycat.manatee@gmail.com")
 :provided   (gnus-summary-stripe)
 :required   ((("stripe-buffer" stripe-buffer)))
 :keywords   (gnus stripe summary)
 :homepage   "http://www.emacswiki.org/GnusSummaryStripe"
 :wikipage   "http://www.emacswiki.org/GnusSummaryStripe"
 :commentary "\
Strip gnus summary buffer


Installation:

Put gnus-summary-stripe.el to your load-path.
The load-path is usually ~/elisp/.
It's set in your ~/.emacs like this:
(add-to-list 'load-path (expand-file-name \"~/elisp\"))

And the following to your ~/.emacs startup file.

(require 'gnus-summary-stripe)

No need more.")
