(:summary    "Extension for isearch"
 :created    "20081222"
 :updated    "20081222"
 :license    "GPL-3"
 :authors    ((nil . "andy stewart lazycat.manatee@gmail.com"))
 :maintainer (nil . "andy stewart lazycat.manatee@gmail.com")
 :provided   (isearch-extension)
 :keywords   (isearch)
 :commentary "\
Extension for isearch.


Installation:

Put isearch-extension.el to your load-path.
The load-path is usually ~/elisp/.
It's set in your ~/.emacs like this:
(add-to-list 'load-path (expand-file-name \"~/elisp\"))

And the following to your ~/.emacs startup file.

(require 'isearch-extension)

No need more.")
